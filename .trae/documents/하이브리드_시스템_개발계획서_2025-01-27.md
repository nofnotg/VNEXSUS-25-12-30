# 하이브리드 시스템 개발 계획서 - visionLLM & vision OCR 통합

**작성일**: 2026-01-27
**프로젝트명**: VNEXSUS 하이브리드 OCR 시스템 개발
**목표**: visionLLM과 vision OCR의 장점을 결합한 하이브리드 시스템 구축

---

## 📋 1. 프로젝트 개요

### 1.1 배경

**현재 상황:**
- **visionLLM (GPT-4o Vision)** 사용 중
  - 장점: 표 구조 인식 우수 (95%), 맥락 이해 우수
  - 단점: 좌표 정보 없음, 비용 높음, 처리 속도 느림

**전환 계획:**
- **vision OCR (Google Vision / AWS Textract)** 전환 예정
  - 장점: 좌표 정보 제공, 비용 저렴, 처리 속도 빠름
  - 단점: 표 구조 인식 약함 (70-80%), 맥락 이해 제한적

**하이브리드 접근의 필요성:**
- 두 시스템의 장점을 결합하여 최적의 성능 달성
- 문서 유형별 최적 OCR 엔진 선택
- 점진적 전환으로 리스크 최소화

### 1.2 목표

**주목표:**
- visionLLM과 vision OCR을 상황별로 선택적으로 사용하는 하이브리드 시스템 구축
- 문서 특성에 따른 지능형 OCR 엔진 라우팅
- 기존 후처리 로직 (Cycle 4-5) 및 보고서 생성 로직 재사용

**부목표:**
- 비용 최적화 (visionLLM 사용 최소화)
- 처리 속도 향상 (vision OCR 우선 사용)
- 정확도 유지/향상 (어려운 케이스는 visionLLM 사용)

---

## 🏗️ 2. 하이브리드 시스템 아키텍처

### 2.1 통합 아키텍처 설계

```
┌──────────────────────────────────────────────────────────┐
│                      PDF 입력                             │
└────────────────────────┬─────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────┐
│              문서 분석기 (Document Analyzer)               │
│  - 페이지별 복잡도 분석                                     │
│  - 표 밀도 측정                                            │
│  - 텍스트 품질 평가                                        │
│  - OCR 엔진 추천                                           │
└────────────────────────┬─────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────┐
│           지능형 라우터 (Intelligent Router)               │
│  ┌─────────────────┐  ┌─────────────────┐               │
│  │  복잡도 < 0.5   │  │  복잡도 >= 0.5  │               │
│  │  표 밀도 < 30% │  │  표 밀도 >= 30% │               │
│  │  → vision OCR   │  │  → visionLLM    │               │
│  └─────────────────┘  └─────────────────┘               │
└──────────┬──────────────────────────┬────────────────────┘
           │                          │
           ▼                          ▼
┌─────────────────────┐    ┌─────────────────────┐
│   vision OCR         │    │   visionLLM         │
│  (Google Vision)     │    │  (GPT-4o Vision)    │
│  - 텍스트 + 좌표     │    │  - 텍스트만          │
│  - 빠름, 저렴        │    │  - 느림, 비쌈        │
└──────────┬───────────┘    └──────────┬──────────┘
           │                          │
           └──────────┬───────────────┘
                      ▼
┌──────────────────────────────────────────────────────────┐
│              출력 통합기 (Output Integrator)               │
│  - 두 OCR 결과를 공통 포맷으로 변환                         │
│  - 좌표 정보 정규화 (visionLLM은 기본값 생성)              │
│  - BindInput 형식으로 변환                                 │
└────────────────────────┬─────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────┐
│         과추출 날짜 추출 + 7-Phase 후처리                   │
│  (Cycle 4-5 로직 재사용)                                   │
└────────────────────────┬─────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────┐
│         dateBindingV3 (좌표 기반 바인딩)                   │
└────────────────────────┬─────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────┐
│         eventPacking + 10항목 보고서 생성                  │
└──────────────────────────────────────────────────────────┘
```

### 2.2 핵심 구성 요소

#### 2.2.1 문서 분석기 (Document Analyzer)

**목적:** 문서 특성을 분석하여 최적 OCR 엔진 선택

**주요 기능:**
```typescript
interface DocumentAnalysis {
  complexity: number;       // 0~1, 높을수록 복잡
  tableDensity: number;     // 0~1, 표가 차지하는 비율
  textQuality: number;      // 0~1, 텍스트 선명도
  pageCount: number;        // 페이지 수
  recommendedEngine: 'vision-ocr' | 'vision-llm' | 'hybrid';
  confidence: number;       // 추천 신뢰도
}

class DocumentAnalyzer {
  async analyze(pdfPath: string): Promise<DocumentAnalysis> {
    // 1. PDF 메타데이터 추출
    const metadata = await this.extractMetadata(pdfPath);

    // 2. 샘플 페이지 분석 (첫 3페이지)
    const samplePages = await this.extractSamplePages(pdfPath, 3);

    // 3. 복잡도 계산
    const complexity = this.calculateComplexity(samplePages);

    // 4. 표 밀도 측정
    const tableDensity = this.measureTableDensity(samplePages);

    // 5. OCR 엔진 추천
    const recommendedEngine = this.recommendEngine(complexity, tableDensity);

    return {
      complexity,
      tableDensity,
      textQuality: metadata.textQuality,
      pageCount: metadata.pageCount,
      recommendedEngine,
      confidence: this.calculateConfidence(complexity, tableDensity)
    };
  }

  private recommendEngine(complexity: number, tableDensity: number): string {
    // 규칙 기반 추천
    if (complexity < 0.5 && tableDensity < 0.3) {
      return 'vision-ocr';  // 간단한 문서 → vision OCR
    } else if (complexity >= 0.7 || tableDensity >= 0.5) {
      return 'vision-llm';  // 복잡한 문서 → visionLLM
    } else {
      return 'hybrid';      // 중간 → 하이브리드 (페이지별 선택)
    }
  }
}
```

#### 2.2.2 지능형 라우터 (Intelligent Router)

**목적:** 문서 분석 결과에 따라 적절한 OCR 엔진으로 라우팅

**라우팅 전략:**

| 문서 특성 | OCR 엔진 선택 | 근거 |
|----------|-------------|------|
| 복잡도 < 0.5, 표 밀도 < 30% | **vision OCR** | 간단한 문서, 비용/속도 우선 |
| 복잡도 >= 0.7, 표 밀도 >= 50% | **visionLLM** | 복잡한 표 구조, 정확도 우선 |
| 중간 (0.5 ~ 0.7) | **하이브리드** | 페이지별 선택 |
| 의료 용어 밀집 (> 50%) | **visionLLM** | 맥락 이해 필요 |
| 스캔 PDF (저품질) | **visionLLM** | 노이즈 처리 우수 |

**구현 예제:**
```typescript
class IntelligentRouter {
  async route(
    pdfPath: string,
    analysis: DocumentAnalysis
  ): Promise<OcrResult> {
    // 1. 전역 설정 확인 (강제 모드)
    const forceMode = process.env.FORCE_OCR_MODE; // 'vision-ocr', 'vision-llm', 'hybrid'
    if (forceMode) {
      return this.routeByMode(pdfPath, forceMode);
    }

    // 2. 문서 분석 결과 기반 라우팅
    switch (analysis.recommendedEngine) {
      case 'vision-ocr':
        return this.routeToVisionOcr(pdfPath);

      case 'vision-llm':
        return this.routeToVisionLlm(pdfPath);

      case 'hybrid':
        return this.routeHybrid(pdfPath, analysis);

      default:
        // 기본값: vision OCR (비용 절감)
        return this.routeToVisionOcr(pdfPath);
    }
  }

  private async routeHybrid(
    pdfPath: string,
    analysis: DocumentAnalysis
  ): Promise<OcrResult> {
    // 페이지별 복잡도 분석
    const pageAnalyses = await this.analyzePages(pdfPath);

    const results = [];
    for (const [pageNum, pageAnalysis] of pageAnalyses.entries()) {
      if (pageAnalysis.complexity >= 0.6 || pageAnalysis.tableDensity >= 0.4) {
        // 복잡한 페이지 → visionLLM
        results.push(await this.processPageWithLlm(pdfPath, pageNum));
      } else {
        // 간단한 페이지 → vision OCR
        results.push(await this.processPageWithOcr(pdfPath, pageNum));
      }
    }

    // 결과 통합
    return this.mergePageResults(results);
  }
}
```

#### 2.2.3 출력 통합기 (Output Integrator)

**목적:** 두 OCR 엔진의 출력을 공통 형식으로 변환

**공통 출력 형식 (BindInput):**
```typescript
interface BindInput {
  dates: DateBox[];      // 추출된 날짜 (좌표 포함)
  blocks: TextBlock[];   // 텍스트 블록 (좌표 포함)
}

interface DateBox {
  text: string;          // "2023-05-15"
  bbox: BBox;            // {page, x, y, width, height}
}

interface TextBlock {
  text: string;
  bbox: BBox;
  page: number;
  confidence: number;
}

interface BBox {
  page: number;
  x: number;             // 0~1 (정규화)
  y: number;             // 0~1 (정규화)
  width: number;         // 0~1 (정규화)
  height: number;        // 0~1 (정규화)
}
```

**변환 로직:**
```typescript
class OutputIntegrator {
  /**
   * visionLLM 결과를 BindInput으로 변환
   */
  integrateVisionLlmOutput(llmResult: VisionLlmResult): BindInput {
    const blocks: TextBlock[] = [];
    const dates: DateBox[] = [];

    for (const page of llmResult.pages) {
      // 1. 텍스트 블록 생성 (좌표는 기본값 생성)
      const pageBlocks = this.createBlocksFromText(
        page.text,
        page.pageNum
      );
      blocks.push(...pageBlocks);

      // 2. 날짜 추출 (정규표현식)
      const pageDates = this.extractDatesFromText(
        page.text,
        page.pageNum
      );
      dates.push(...pageDates);
    }

    return { dates, blocks };
  }

  /**
   * vision OCR 결과를 BindInput으로 변환
   */
  integrateVisionOcrOutput(ocrResult: VisionOcrResult): BindInput {
    // ocrParser.ts 로직 재사용
    const blocks = ocrParser.extractTextBlocks(ocrResult);
    const dates = ocrParser.extractDates(blocks);

    return { dates, blocks };
  }

  /**
   * 하이브리드 결과 통합
   */
  integrateHybridOutput(
    ocrPages: VisionOcrResult[],
    llmPages: VisionLlmResult[]
  ): BindInput {
    const allBlocks: TextBlock[] = [];
    const allDates: DateBox[] = [];

    // OCR 결과 통합
    for (const ocrPage of ocrPages) {
      const { dates, blocks } = this.integrateVisionOcrOutput(ocrPage);
      allBlocks.push(...blocks);
      allDates.push(...dates);
    }

    // LLM 결과 통합
    for (const llmPage of llmPages) {
      const { dates, blocks } = this.integrateVisionLlmOutput(llmPage);
      allBlocks.push(...blocks);
      allDates.push(...dates);
    }

    // 중복 제거 (같은 페이지의 같은 날짜)
    const uniqueDates = this.deduplicateDates(allDates);

    return { dates: uniqueDates, blocks: allBlocks };
  }

  /**
   * visionLLM 텍스트에서 블록 생성 (좌표 기본값)
   */
  private createBlocksFromText(text: string, pageNum: number): TextBlock[] {
    const lines = text.split('\n');
    const blocks: TextBlock[] = [];

    for (let i = 0; i < lines.length; i++) {
      if (!lines[i].trim()) continue;

      blocks.push({
        text: lines[i].trim(),
        bbox: {
          page: pageNum,
          x: 0.1,
          y: 0.1 + (i * 0.03),  // 간격 0.03씩 증가
          width: 0.8,
          height: 0.03
        },
        page: pageNum,
        confidence: 1.0
      });
    }

    return blocks;
  }
}
```

---

## 🔨 3. 단계별 개발 계획

### 3.1 Phase 1: 문서 분석기 및 라우터 구축 (1주)

#### 목표
- 문서 복잡도 분석 로직 구현
- 기본 라우팅 로직 구현
- vision OCR / visionLLM 호출 인터페이스 통합

#### 주요 작업

**1. 문서 분석기 구현**
```typescript
// src/modules/hybrid-ocr/service/documentAnalyzer.ts (신규)
export class DocumentAnalyzer {
  async analyze(pdfPath: string): Promise<DocumentAnalysis>;
  private calculateComplexity(pages: Page[]): number;
  private measureTableDensity(pages: Page[]): number;
  private extractMetadata(pdfPath: string): Promise<Metadata>;
}
```

**2. 지능형 라우터 구현**
```typescript
// src/modules/hybrid-ocr/service/intelligentRouter.ts (신규)
export class IntelligentRouter {
  async route(pdfPath: string, analysis: DocumentAnalysis): Promise<OcrResult>;
  private routeToVisionOcr(pdfPath: string): Promise<OcrResult>;
  private routeToVisionLlm(pdfPath: string): Promise<OcrResult>;
  private routeHybrid(pdfPath: string, analysis: DocumentAnalysis): Promise<OcrResult>;
}
```

**3. OCR 서비스 통합**
```typescript
// src/modules/hybrid-ocr/service/ocrServiceManager.ts (신규)
export class OcrServiceManager {
  private visionOcrService: VisionOcrService;
  private visionLlmService: VisionLlmService;

  async callVisionOcr(pdfPath: string): Promise<VisionOcrResult>;
  async callVisionLlm(pdfPath: string): Promise<VisionLlmResult>;
  async getServiceStatus(): Promise<ServiceStatus>;
}
```

#### 검증 방법
- [ ] 5개 샘플 PDF 분석 정확도 테스트
- [ ] 라우팅 로직 단위 테스트
- [ ] 두 OCR 엔진 호출 성공 확인

---

### 3.2 Phase 2: 출력 통합기 및 후처리 연결 (1주)

#### 목표
- 두 OCR 엔진 출력을 BindInput 형식으로 변환
- 기존 Cycle 4-5 후처리 로직 연결
- dateBindingV3 연결

#### 주요 작업

**1. 출력 통합기 구현**
```typescript
// src/modules/hybrid-ocr/adapter/outputIntegrator.ts (신규)
export class OutputIntegrator {
  integrateVisionLlmOutput(llmResult: VisionLlmResult): BindInput;
  integrateVisionOcrOutput(ocrResult: VisionOcrResult): BindInput;
  integrateHybridOutput(ocrPages: any[], llmPages: any[]): BindInput;
}
```

**2. 후처리 어댑터 구현**
```typescript
// backend/adapters/hybridPostProcessingAdapter.js (신규)
class HybridPostProcessingAdapter {
  async process(bindInput: BindInput): Promise<FilteredEvents> {
    // 1. 과추출 날짜 추출 (ocrAdapter 사용)
    const dates = extractDatesWithOverExtraction(bindInput.blocks);

    // 2. 7-Phase 후처리 적용
    const filtered = await apply7PhasePostProcessing(dates);

    // 3. dateBindingV3 적용
    const events = bindDatesToEvents({ dates: filtered, blocks: bindInput.blocks });

    return events;
  }
}
```

#### 검증 방법
- [ ] visionLLM 출력 → BindInput 변환 테스트
- [ ] vision OCR 출력 → BindInput 변환 테스트
- [ ] 후처리 파이프라인 통합 테스트

---

### 3.3 Phase 3: E2E 통합 및 UI 추가 (1주)

#### 목표
- 전체 파이프라인 E2E 테스트
- UI에서 OCR 모드 선택 기능 추가
- 성능 모니터링 추가

#### 주요 작업

**1. E2E 파이프라인 구현**
```typescript
// backend/services/hybridOcrPipeline.js (신규)
class HybridOcrPipeline {
  async process(pdfPath: string, options?: HybridOptions): Promise<Report> {
    // 1. 문서 분석
    const analysis = await this.documentAnalyzer.analyze(pdfPath);

    // 2. OCR 라우팅
    const ocrResult = await this.router.route(pdfPath, analysis);

    // 3. 출력 통합
    const bindInput = this.integrator.integrate(ocrResult);

    // 4. 후처리 (Cycle 4-5)
    const events = await this.postProcessor.process(bindInput);

    // 5. 이벤트 패킹
    const packed = this.eventPacker.pack(events);

    // 6. 10항목 보고서 생성
    const report = await this.reportGenerator.generate(packed);

    return report;
  }
}
```

**2. UI 모드 선택 추가**
```jsx
// frontend/components/OcrModeSelector.jsx (신규)
function OcrModeSelector({ onChange }) {
  return (
    <div className="ocr-mode-selector">
      <label>OCR 엔진 선택:</label>
      <select onChange={(e) => onChange(e.target.value)}>
        <option value="auto">자동 선택 (권장)</option>
        <option value="vision-ocr">vision OCR (빠름, 저렴)</option>
        <option value="vision-llm">visionLLM (정확함, 비쌈)</option>
        <option value="hybrid">하이브리드 (페이지별)</option>
      </select>
      <div className="mode-info">
        {/* 각 모드 설명 */}
      </div>
    </div>
  );
}
```

**3. 성능 모니터링 대시보드**
```typescript
// backend/monitoring/hybridMetrics.js (신규)
class HybridMetrics {
  trackOcrUsage(engine: string, duration: number, cost: number) {
    this.metrics.push({
      engine,
      duration,
      cost,
      timestamp: Date.now()
    });
  }

  getStatistics(period: string): Statistics {
    return {
      visionOcrUsage: this.calculateUsage('vision-ocr', period),
      visionLlmUsage: this.calculateUsage('vision-llm', period),
      hybridUsage: this.calculateUsage('hybrid', period),
      totalCost: this.calculateTotalCost(period),
      avgAccuracy: this.calculateAvgAccuracy(period)
    };
  }
}
```

#### 검증 방법
- [ ] 전체 케이스 (50개) E2E 테스트
- [ ] UI에서 모드 선택 및 실행 테스트
- [ ] 성능 지표 수집 및 분석

---

## 📊 4. 비용 및 성능 분석

### 4.1 비용 비교

| OCR 엔진 | 비용/페이지 | 처리 속도 | 정확도 | 적합 문서 |
|---------|-----------|---------|-------|---------|
| **vision OCR** | $0.001 | 0.5s/page | 85% | 간단한 문서 |
| **visionLLM** | $0.01 | 3s/page | 95% | 복잡한 표 |
| **하이브리드** | $0.003 | 1s/page | 92% | 혼합 문서 |

**비용 절감 시뮬레이션:**
- 전체 문서: 1,000건
- 평균 페이지: 10페이지/건
- 총 페이지: 10,000페이지

| 시나리오 | vision OCR | visionLLM | 하이브리드 | 절감액 |
|---------|-----------|-----------|----------|-------|
| **100% vision OCR** | $10 | - | - | - |
| **100% visionLLM** | - | $100 | - | - |
| **하이브리드 (70% OCR, 30% LLM)** | $7 | $30 | **$37** | **$63 (63%)** |

### 4.2 성능 목표

| 지표 | 현재 (visionLLM) | 목표 (하이브리드) | 개선율 |
|-----|-----------------|------------------|-------|
| **비용** | $100 | $40 | **-60%** |
| **처리 속도** | 30s/PDF | 15s/PDF | **+100%** |
| **GT Coverage** | 57% | 65% | **+14%** |
| **Precision** | 24% | 50% | **+108%** |
| **날짜 바인딩 정확도** | 45% | 85% | **+89%** |

---

## 🎯 5. 하이브리드 전략

### 5.1 문서 유형별 OCR 엔진 선택

| 문서 유형 | OCR 엔진 | 근거 |
|----------|---------|------|
| **간단한 진료기록** | vision OCR | 표 적음, 텍스트 중심 |
| **수술 기록 (표 많음)** | visionLLM | 복잡한 표 구조 |
| **병리 검사 결과** | visionLLM | 전문 용어 많음 |
| **입원 요약지** | 하이브리드 | 표 + 일반 텍스트 혼합 |
| **외래 진료 노트** | vision OCR | 간단한 구조 |
| **영상 검사 판독문** | vision OCR | 텍스트 중심 |
| **보험 청구서** | visionLLM | 표 구조 복잡 |

### 5.2 페이지별 선택 전략 (하이브리드 모드)

**페이지 분류 기준:**
```typescript
interface PageClassification {
  pageNum: number;
  complexity: number;      // 0~1
  tableDensity: number;    // 0~1
  medicalTermDensity: number;  // 0~1
  recommendedEngine: 'vision-ocr' | 'vision-llm';
}

function classifyPage(page: Page): PageClassification {
  const complexity = calculatePageComplexity(page);
  const tableDensity = measurePageTableDensity(page);
  const medicalTermDensity = measureMedicalTermDensity(page);

  // 결정 로직
  if (complexity >= 0.6 || tableDensity >= 0.4 || medicalTermDensity >= 0.5) {
    return 'vision-llm';
  } else {
    return 'vision-ocr';
  }
}
```

### 5.3 비용 최적화 전략

**1. vision OCR 우선 사용**
- 기본값: vision OCR
- 실패 시에만 visionLLM으로 폴백

**2. 캐싱 전략**
- OCR 결과 캐싱 (같은 PDF는 재처리 안함)
- 분석 결과 캐싱 (문서 특성 재분석 안함)

**3. 배치 처리**
- 간단한 문서는 배치로 vision OCR 처리
- 복잡한 문서만 개별 visionLLM 처리

---

## 🧪 6. 테스트 계획

### 6.1 단위 테스트

**1. 문서 분석기 테스트**
```typescript
describe('DocumentAnalyzer', () => {
  it('should classify simple document as vision-ocr', async () => {
    const analysis = await analyzer.analyze('simple.pdf');
    expect(analysis.recommendedEngine).toBe('vision-ocr');
    expect(analysis.complexity).toBeLessThan(0.5);
  });

  it('should classify complex table document as vision-llm', async () => {
    const analysis = await analyzer.analyze('complex-table.pdf');
    expect(analysis.recommendedEngine).toBe('vision-llm');
    expect(analysis.tableDensity).toBeGreaterThan(0.5);
  });
});
```

**2. 라우터 테스트**
```typescript
describe('IntelligentRouter', () => {
  it('should route to vision OCR for simple document', async () => {
    const analysis = { recommendedEngine: 'vision-ocr', complexity: 0.3 };
    const result = await router.route('simple.pdf', analysis);
    expect(result.engine).toBe('vision-ocr');
  });
});
```

**3. 출력 통합기 테스트**
```typescript
describe('OutputIntegrator', () => {
  it('should convert visionLLM output to BindInput', () => {
    const llmResult = loadMockLlmResult();
    const bindInput = integrator.integrateVisionLlmOutput(llmResult);
    expect(bindInput.dates).toHaveLength(greaterThan(0));
    expect(bindInput.blocks[0].bbox).toBeDefined();
  });
});
```

### 6.2 통합 테스트

**시나리오별 테스트:**

| 시나리오 | 문서 유형 | 예상 OCR 엔진 | 검증 항목 |
|---------|---------|-------------|---------|
| 1 | 간단한 진료 기록 | vision OCR | 처리 속도, 비용 |
| 2 | 복잡한 수술 기록 | visionLLM | 표 인식 정확도 |
| 3 | 혼합 문서 (10페이지) | 하이브리드 | 페이지별 엔진 선택 |
| 4 | 저품질 스캔 문서 | visionLLM | 노이즈 처리 |
| 5 | 의료 용어 밀집 문서 | visionLLM | 맥락 이해 |

### 6.3 성능 벤치마크

**측정 지표:**
```typescript
interface BenchmarkResult {
  caseId: string;
  ocrEngine: string;
  processingTime: number;
  cost: number;
  gtCoverage: number;
  precision: number;
  dateBindingAccuracy: number;
  reportGenerationSuccess: boolean;
}

// 벤치마크 실행
async function runBenchmark(cases: TestCase[]): Promise<BenchmarkResult[]> {
  const results = [];
  for (const testCase of cases) {
    const result = await hybridPipeline.process(testCase.pdfPath);
    results.push({
      caseId: testCase.id,
      ocrEngine: result.ocrEngine,
      processingTime: result.duration,
      cost: calculateCost(result),
      gtCoverage: evaluateGtCoverage(result, testCase.groundTruth),
      precision: evaluatePrecision(result, testCase.groundTruth),
      dateBindingAccuracy: evaluateBindingAccuracy(result),
      reportGenerationSuccess: result.report !== null
    });
  }
  return results;
}
```

---

## 📅 7. 일정 및 마일스톤

### 7.1 전체 일정 (총 3주)

| 주차 | Phase | 주요 작업 | 검증 기준 |
|-----|-------|---------|---------|
| **1주차** | Phase 1 | 문서 분석기 + 라우터 구축 | 5개 샘플 테스트 통과 |
| **2주차** | Phase 2 | 출력 통합기 + 후처리 연결 | 후처리 파이프라인 통합 |
| **3주차** | Phase 3 | E2E 통합 + UI + 모니터링 | 전체 케이스 테스트 통과 |

### 7.2 체크리스트

#### Week 1: 문서 분석기 및 라우터
- [ ] Day 1-2: DocumentAnalyzer 구현
- [ ] Day 3-4: IntelligentRouter 구현
- [ ] Day 5: OcrServiceManager 구현
- [ ] Day 6-7: 단위 테스트 및 샘플 테스트

#### Week 2: 출력 통합기 및 후처리
- [ ] Day 1-2: OutputIntegrator 구현
- [ ] Day 3: HybridPostProcessingAdapter 구현
- [ ] Day 4-5: 후처리 파이프라인 연결
- [ ] Day 6-7: 통합 테스트

#### Week 3: E2E 통합 및 배포
- [ ] Day 1-2: HybridOcrPipeline 구현
- [ ] Day 3: UI 모드 선택 기능 추가
- [ ] Day 4: 성능 모니터링 추가
- [ ] Day 5-6: 전체 케이스 E2E 테스트
- [ ] Day 7: 최종 검증 및 문서화

---

## 🎯 8. 성공 기준 (KPI)

### 8.1 비용 목표

- ✅ **총 비용 60% 절감** (visionLLM 대비)
  - 현재: $100/1000건
  - 목표: $40/1000건

- ✅ **vision OCR 사용률 70% 이상**
  - 간단한 문서 자동 선택으로 비용 절감

### 8.2 성능 목표

- ✅ **처리 속도 2배 향상**
  - 현재: 30s/PDF
  - 목표: 15s/PDF

- ✅ **GT Coverage 65% 이상**
  - 현재: 57%
  - 목표: 65%+

- ✅ **Precision 50% 이상**
  - 현재: 24%
  - 목표: 50%+

### 8.3 품질 목표

- ✅ **날짜 바인딩 정확도 85% 이상**
  - 현재: 45%
  - 목표: 85%+ (좌표 정보 활용)

- ✅ **보고서 생성 성공률 95% 이상**
  - 현재: 90%
  - 목표: 95%+

---

## 🔒 9. 리스크 관리

### 9.1 기술적 리스크

#### 1. 문서 분석 정확도 부족
- **리스크**: 복잡도 분석 오류로 잘못된 OCR 엔진 선택
- **완화**: 보수적 임계값 설정 (애매하면 visionLLM)
- **대응**: 사용자 피드백 수집 후 임계값 튜닝

#### 2. vision OCR 표 인식 실패
- **리스크**: 예상보다 표 인식 정확도 낮음
- **완화**: 하이브리드 모드에서 표 밀도 높은 페이지는 visionLLM 사용
- **대응**: 실패 케이스 분석 후 분류 기준 조정

#### 3. 좌표 정보 품질 저하
- **리스크**: visionLLM 결과에 기본 좌표 생성 시 정확도 저하
- **완화**: 텍스트 기반 Fallback 로직 유지
- **대응**: visionLLM 페이지는 좌표 의존도 낮은 로직 사용

### 9.2 운영 리스크

#### 1. 비용 초과
- **리스크**: visionLLM 사용률이 예상보다 높아 비용 초과
- **완화**: 일일 비용 모니터링 및 알림 설정
- **대응**: 임계값 조정으로 vision OCR 사용률 증가

#### 2. 성능 저하
- **리스크**: 분석 오버헤드로 전체 처리 시간 증가
- **완화**: 분석 결과 캐싱, 비동기 처리
- **대응**: 분석 생략 모드 제공 (강제 vision OCR)

---

## 📝 10. 결론 및 다음 단계

### 10.1 기대 효과

1. **비용 절감**: visionLLM 대비 60% 비용 절감
2. **처리 속도 향상**: 2배 빠른 처리 (30s → 15s/PDF)
3. **정확도 향상**: 좌표 정보 활용으로 날짜 바인딩 정확도 89% 향상
4. **유연성**: 문서 특성에 따른 최적 OCR 엔진 선택

### 10.2 장기 비전

**Phase 4: 머신러닝 기반 최적화 (3개월 후)**
- 문서 분석을 ML 모델로 고도화
- 사용자 피드백 기반 자동 학습
- OCR 엔진 선택 정확도 95% 이상 달성

**Phase 5: 엣지 컴퓨팅 통합 (6개월 후)**
- 로컬 OCR 엔진 통합 (Tesseract, EasyOCR)
- 민감 정보 처리 시 클라우드 사용 최소화
- 프라이버시 보호 강화

### 10.3 즉시 실행 항목

1. ✅ 개발 환경 설정
2. ✅ 샘플 PDF 5개 준비 (다양한 복잡도)
3. ✅ DocumentAnalyzer 구현 시작
4. ✅ OCR 서비스 계정 설정 (Google Vision, OpenAI)

---

## 📚 부록

### A. 참고 파일

| 파일 경로 | 설명 |
|----------|-----|
| `backend/services/visionLLMService.js` | 현재 visionLLM 서비스 |
| `backend/services/visionService.js` | Google Vision OCR 서비스 |
| `src/lib/ocrParser.ts` | Google Vision OCR 파서 |
| `backend/eval/cycle5PostProcessing.js` | 7-Phase 후처리 |
| `src/modules/medical-events/service/dateBindingV3.ts` | 날짜 바인딩 v3 |

### B. 용어 정리

- **visionLLM**: GPT-4o Vision API 기반 OCR (좌표 없음, 표 인식 우수)
- **vision OCR**: Google Vision / AWS Textract 기반 OCR (좌표 있음)
- **하이브리드**: 두 OCR 엔진을 상황별로 선택적 사용
- **문서 분석기**: 문서 특성을 분석하여 최적 OCR 엔진 추천
- **지능형 라우터**: 분석 결과에 따라 OCR 엔진으로 라우팅
- **출력 통합기**: 두 OCR 엔진 출력을 공통 형식으로 변환

### C. 문서 정보

- **작성자**: Claude Agent
- **작성일**: 2026-01-27
- **버전**: v2.0 (vision OCR 전환 대응)
- **이전 버전**: v1.0 (일반 하이브리드)
- **관련 문서**: 후처리·보고서 안정화 지속 실행 v1.1 계획.md
