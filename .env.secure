# VNEXSUS 보안 강화 환경변수 설정
# 주의: 실제 API 키는 이 파일에 저장하지 마세요!

NODE_ENV=development
PORT=3030

# 코어 엔진 사용 여부 (토글)
USE_CORE_ENGINE=true

# Enable Google Cloud Vision OCR
ENABLE_VISION_OCR=true
USE_VISION=true
USE_TEXTRACT=false

# Google Cloud Vision OCR Configuration
GCS_BUCKET_NAME=medreport-vision-ocr-bucket
GCS_UPLOAD_PREFIX=temp-uploads/
GCP_PROJECT_ID=medreport-assistant
# 보안: 실제 경로는 환경별로 다르게 설정
GOOGLE_APPLICATION_CREDENTIALS=${GOOGLE_CREDENTIALS_PATH}
# 보안: 실제 API 키는 Secret Manager 사용
GOOGLE_CLOUD_VISION_API_KEY=${GOOGLE_VISION_KEY}

# Google Cloud Pub/Sub Configuration
PUBSUB_TOPIC=projects/medreport-assistant/topics/mediai

# OpenAI API Configuration
# 보안: 실제 API 키는 Secret Manager 사용
OPENAI_API_KEY=${OPENAI_KEY}
OPENAI_MODEL=gpt-4o-mini
OPENAI_TEMPERATURE=0.2

# 코어 엔진 설정
DISCLOSURE_WINDOWS=3m,2y,5y
DEFAULT_CLAIM_DIAGNOSIS=""

# 보안 설정
CORS_ORIGIN=http://localhost:3030
MAX_FILE_SIZE=209715200
MAX_FILES_PER_REQUEST=5
RATE_LIMIT_REQUESTS_PER_MINUTE=60

# 로깅 설정
LOG_LEVEL=info
DEBUG_MODE=false

# 테스트 환경 설정
SKIP_PDF_TESTS=false
TEST_DATA_PATH=./tests/data

# 모니터링 설정
ENABLE_METRICS=true
METRICS_PORT=9090

# CORS origins (same-origin recommended; backend serves frontend)
# FRONTEND_PORT=3030