# OCR 날짜 오류 정밀 분석 보고서

## 📋 Executive Summary

**핵심 발견: "오류"는 OCR 인식 오류가 아니라 OCR 누락(False Negative)**

- 분석 대상: 28개 케이스 중 하/중 등급 13개 케이스
- 총 43개 누락 날짜 분석 완료
- **100% 모두 OCR 블록에서 발견되지 않음** → Baseline에만 존재
- 이는 OCR이 날짜를 "잘못 읽은 것"이 아니라 "아예 추출하지 못한 것"

---

## 🔍 1. 상세 분석 결과

### 1.1 오류 유형 재정의

**기존 가정 (❌ 틀림):**
- OCR이 날짜를 잘못 인식함 (예: 2025를 2054로 오인식)
- 날짜가 아닌 숫자를 날짜로 오인식
- 불가능한 날짜 생성 (예: 2024-00-24)

**실제 발견 (✅ 맞음):**
- OCR이 문서에 존재하는 날짜를 **추출하지 못함** (False Negative)
- Baseline 보고서는 사람이 원본 문서를 검토하여 작성
- 누락된 날짜들은 모두 중요한 계약/보험 정보

### 1.2 정량 분석

```
총 Baseline 날짜:    118개  (사람이 추출한 중요 날짜)
총 OCR 날짜:         656개  (OCR이 추출한 모든 날짜)
매칭:                 75개  (63.6%)
OCR 누락:             43개  (36.4%) ← 핵심 문제
OCR 추가 검출:       581개  (Baseline에 없는 날짜)
```

**해석:**
- OCR은 "aggressive" - 문서의 모든 날짜를 추출하려 함 (656개)
- 하지만 "imprecise" - Baseline의 중요 날짜 36.4%를 놓침
- Baseline은 "selective" - 보험 청구에 필요한 핵심 날짜만 선별 (118개)

### 1.3 누락 패턴 분석

**컨텍스트 키워드 빈도:**
```
보험:   27회 (62.8%)
가입:   16회 (37.2%)
~:      11회 (25.6%) ← 기간 표시
```

**시간대 분포:**
```
과거 날짜 (<2024):     20개 (46.5%) - 과거 가입 이력
최근 날짜 (2024-2025): 19개 (44.2%) - 최근 계약
미래 날짜 (2026+):      4개 ( 9.3%) - 보험 만기일
```

**누락 사례:**

1. **보험 기간 (가장 흔한 패턴)**
   ```
   Case10 Line 7-8:
   보 험 기 간 ① 2024.05.01 ~ 2054.11.10  ← 2024.05.01, 2054.11.10 누락
              ② 2024.05.01 ~ 2025.11.10  ← 2025.11.10 누락
   ```

2. **가입 날짜**
   ```
   Case1 Line 35:
   2024.09.27 무배당 AXA 나를지켜주는건강보험(갱신형)2407 가입 AXA 손해보험
   ↑ 누락
   ```

3. **기간 표시**
   ```
   Case15 Line 2:
   2025.02.25 ~ 2053.02.25|나에게 맞춘 초경증 간편건강보험2407
   ↑ 시작일      ↑ 종료일 (둘 다 누락)
   ```

---

## 🎯 2. 왜 OCR이 이 날짜들을 놓쳤을까?

### 가설 1: 표 구조 파싱 실패 (가능성 높음 ⭐⭐⭐)

**증거:**
- "보 험 기 간" 처럼 글자 사이 공백이 많음 → 표 셀 구조
- "①", "②" 같은 특수 문자 → 항목 나열
- "~" 기호 → 기간 구분

**문제:**
```
OCR 블록 구조:
Block 1: "보 험 기 간"
Block 2: "①"
Block 3: "2024"     ← 날짜가 여러 블록으로 분산
Block 4: "."
Block 5: "05"
Block 6: "."
Block 7: "01"
Block 8: "~"
Block 9: "2054"
...
```

→ 각 블록이 짧아서 날짜 패턴 매칭 실패

### 가설 2: 이미지 품질/레이아웃 문제 (가능성 중간 ⭐⭐)

**증거:**
- 표나 레이아웃이 복잡한 영역에서 누락
- OCR confidence가 낮았을 가능성

**확인 필요:**
- 실제 PDF/이미지에서 해당 영역 확인
- OCR confidence 값 분석

### 가설 3: 날짜 추출 로직 한계 (가능성 낮음 ⭐)

**증거:**
- OCR은 656개 날짜를 추출했음 → 날짜 인식 자체는 잘 됨
- 단, 특정 포맷/위치에서만 누락

---

## 💡 3. 100% 정확도 달성 전략

### A. Quick Win (1주 이내, 즉시 구현 가능)

#### 1️⃣ LLM 기반 날짜 보완

**아이디어:**
현재 파이프라인에 LLM을 이미 사용 중이므로, LLM에게 누락된 날짜를 찾도록 지시

**구현:**
```typescript
// Step 1: OCR 날짜 추출
const ocrDates = extractDatesFromOCR(blocks);

// Step 2: LLM에게 전체 텍스트에서 중요 날짜 찾기 요청
const llmPrompt = `
다음은 의료보험 손해사정 보고서입니다.
중요한 날짜를 모두 찾아주세요:
- 보험 계약일
- 보험 가입일
- 보험 기간 (시작일, 종료일)
- 사고 발생일
- 병원 내원일/입원일/퇴원일

텍스트:
${mergedText}

JSON 형식으로 출력:
{
  "dates": [
    {"date": "YYYY-MM-DD", "type": "계약일", "context": "..."}
  ]
}
`;

const llmDates = await callLLM(llmPrompt);

// Step 3: OCR과 LLM 결과 병합
const allDates = mergeDates(ocrDates, llmDates);
```

**예상 효과:**
- 누락률 36.4% → 10% 이하로 감소
- 비용: LLM 호출 1회/케이스 (이미 호출 중)
- 시간: 1-2일 구현

#### 2️⃣ 날짜 정규화 강화

**현재 문제:**
```python
# 현재는 하이픈만 매칭
pattern = r'(\d{4})-(\d{1,2})-(\d{1,2})'
```

**개선:**
```python
# 모든 구분자 지원
patterns = [
    r'(\d{4})[-](\d{1,2})[-](\d{1,2})',  # 2024-05-01
    r'(\d{4})[.](\d{1,2})[.](\d{1,2})',  # 2024.05.01 ← 추가
    r'(\d{4})[/](\d{1,2})[/](\d{1,2})',  # 2024/05/01 ← 추가
    r'(\d{4})\s*년\s*(\d{1,2})\s*월\s*(\d{1,2})\s*일',
]
```

**예상 효과:**
- 일부 누락 날짜 자동 복원 (점/슬래시 포맷)
- 즉시 적용 가능

#### 3️⃣ 블록 병합 후 날짜 재추출

**아이디어:**
인접한 블록을 병합하여 날짜 패턴 복원

**구현:**
```typescript
function mergeAdjacentBlocks(blocks: Block[]): Block[] {
  const merged = [];

  for (let i = 0; i < blocks.length; i++) {
    // 같은 페이지, Y 좌표 유사한 블록 병합
    if (i > 0 &&
        blocks[i].page === blocks[i-1].page &&
        Math.abs(blocks[i].y - blocks[i-1].y) < 5) {
      // 병합
      merged[merged.length - 1].text += ' ' + blocks[i].text;
    } else {
      merged.push({...blocks[i]});
    }
  }

  return merged;
}

// 날짜 추출 전에 블록 병합
const mergedBlocks = mergeAdjacentBlocks(blocks);
const dates = extractDates(mergedBlocks);
```

**예상 효과:**
- 분산된 날짜 복원 (예: "2024" "." "05" "." "01" → "2024.05.01")
- 복잡도: 중간

---

### B. Medium Win (1개월, 구조적 개선)

#### 1️⃣ 문맥 기반 날짜 우선순위

**아이디어:**
키워드 주변의 날짜에 가중치 부여

**구현:**
```typescript
const importantKeywords = [
  '보험기간', '계약일', '가입일', '청구일',
  '내원일', '입원일', '퇴원일', '진단일'
];

function extractDatesWithContext(blocks: Block[]) {
  const dates = [];

  for (let i = 0; i < blocks.length; i++) {
    const block = blocks[i];

    // 키워드 확인
    const hasKeyword = importantKeywords.some(kw => block.text.includes(kw));

    // 주변 블록 (±3) 확인
    const contextBlocks = blocks.slice(
      Math.max(0, i - 3),
      Math.min(blocks.length, i + 4)
    );

    // 주변에서 날짜 추출
    for (const cb of contextBlocks) {
      const extracted = extractDates(cb.text);
      for (const date of extracted) {
        dates.push({
          date,
          priority: hasKeyword ? 'high' : 'low',
          context: block.text
        });
      }
    }
  }

  return dates;
}
```

#### 2️⃣ 날짜 쌍 인식

**아이디어:**
"시작일 ~ 종료일" 패턴 인식

**구현:**
```typescript
function extractDateRanges(text: string) {
  const rangePattern = /(\d{4}[-.\/]\d{1,2}[-.\/]\d{1,2})\s*~\s*(\d{4}[-.\/]\d{1,2}[-.\/]\d{1,2})/g;

  const ranges = [];
  let match;

  while ((match = rangePattern.exec(text)) !== null) {
    ranges.push({
      start: normalizeDate(match[1]),
      end: normalizeDate(match[2]),
      type: 'period'
    });
  }

  return ranges;
}
```

---

### C. Long-term Win (3개월, AI 고도화)

#### 1️⃣ 문서 구조 인식 + LLM

**아이디어:**
문서를 섹션별로 분리하여 처리

```typescript
// 섹션 감지
const sections = {
  contract: findSection(blocks, ['계약', '담보', '보험종목']),
  claim: findSection(blocks, ['청구', '사고']),
  medical: findSection(blocks, ['병력', '진료', '의무기록'])
};

// 섹션별 LLM 프롬프트
for (const [name, blocks] of Object.entries(sections)) {
  const prompt = getPromptForSection(name, blocks);
  const result = await callLLM(prompt);
  // ...
}
```

#### 2️⃣ 도메인 특화 날짜 추론

**아이디어:**
의료보험 도메인 지식 활용

```typescript
// 예: 보험기간 = 계약일 + 보장기간
const contractDate = findDate(text, '계약일');
const period = extractPeriod(text); // "30년"

if (contractDate && period) {
  const endDate = addYears(contractDate, period);
  // 이 endDate가 baseline에 있는지 확인
}
```

---

## 📊 4. 기대 효과

### Quick Win 구현 후 예상:

| 지표 | 현재 | Quick Win 후 | 개선 |
|------|------|---------------|------|
| 평균 정확도 | 61.7% | **85%+** | +23.3%p |
| 하 등급 케이스 | 5개 | **0-1개** | -4개 |
| 중 등급 케이스 | 8개 | **3-4개** | -4개 |
| 상 등급 케이스 | 15개 | **23-24개** | +8개 |
| OCR 누락률 | 36.4% | **<15%** | -21.4%p |

### 비용:
- 개발 시간: 2-3일
- 추가 API 비용: 거의 없음 (LLM 이미 사용 중)
- ROI: 매우 높음

---

## 🚀 5. 다음 단계 제안

### Option A: Quick Win 즉시 구현 (권장 ⭐⭐⭐)

**일정:**
- Day 1: LLM 날짜 보완 로직 구현
- Day 2: 날짜 정규화 강화 + 블록 병합
- Day 3: 28개 케이스 재검증 + 결과 비교

**장점:**
- 즉시 실행 가능
- 큰 개선 효과 기대
- 기존 파이프라인 최소 변경

### Option B: 근본 원인 추가 조사 (선택)

**조사 항목:**
1. 실제 PDF 이미지에서 누락 영역 확인
2. OCR confidence 값 분석
3. 블록 좌표 분포 시각화

**목적:**
- OCR 누락 원인 정확히 파악
- 장기 개선 방향 수립

**시간:** 1-2일

---

## 💬 6. 토론 포인트

### Q1. 어떤 접근이 가장 실용적인가?

**제 의견:**
- Quick Win (Option A)를 먼저 구현
- 효과를 측정한 후 Medium/Long-term 결정
- 이유: 80/20 법칙 - 20% 노력으로 80% 개선 가능

### Q2. Baseline의 정확성은?

**현재 가정:**
- Baseline은 사람이 검토한 정확한 날짜

**확인 필요:**
- Baseline 작성 프로세스 검증
- 일부 케이스 샘플링하여 원본 문서와 대조

### Q3. OCR 추가 검출 581개는 노이즈인가?

**분석 필요:**
- 이 날짜들이 실제로 문서에 있는지?
- Baseline이 선별적이라면, OCR의 "과다 검출"은 문제가 아닐 수도

**확인 방법:**
- 일부 "추가 검출" 날짜 샘플링
- 원본 문서에서 확인

---

## 📝 7. 결론

**핵심 발견:**
1. ❌ "OCR 인식 오류"가 아님
2. ✅ "OCR 누락 (False Negative)"이 핵심 문제
3. 💡 LLM 활용으로 즉시 개선 가능

**추천:**
- Quick Win 구현으로 단기간 내 85%+ 정확도 달성
- 비용 대비 효과가 가장 높음
- 검증 후 추가 개선 방향 결정

**당신의 피드백을 기다립니다:**
1. Quick Win 구현에 동의하시나요?
2. 추가 조사가 필요한 부분이 있나요?
3. 다른 우려사항이나 아이디어가 있나요?
