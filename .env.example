# ================================================================
# MediAI MVP_v6 Environment Variables
# ⚠️ 중요: 이 파일을 복사하여 .env 파일을 생성하고 실제 값으로 변경하세요
# 🚨 절대 변경 금지: PORT, NODE_ENV, OCR 설정들
# ================================================================

# ================================================================
# 🚨 서버 설정 (절대 변경 금지 - CORS 오류 방지)
# ================================================================
NODE_ENV=production
PORT=8888
FRONTEND_PORT=5173

# ================================================================
# 🔑 Google Cloud 설정 (OCR 핵심 - 필수 설정)
# ================================================================
# GCS 설정
GCS_BUCKET_NAME=your-gcs-bucket-name
GCP_PROJECT_ID=your-gcp-project-id
GCS_UPLOAD_PREFIX=temp-uploads/

# 🚨 Vision OCR 설정 (절대 변경 금지 - 시스템 핵심 설정)
ENABLE_VISION_OCR=true
USE_VISION=true
USE_TEXTRACT=false
GOOGLE_APPLICATION_CREDENTIALS=C:/VisionKeys/your-service-account-key.json
GOOGLE_CLOUD_VISION_API_KEY=your_vision_api_key

# ================================================================
# 🔑 Supabase 설정 (필수 설정)
# ================================================================
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# ================================================================
# 🔑 AI 서비스 설정 (환경별 설정 필요)
# ================================================================
# Claude AI (주요 AI 모델)
ANTHROPIC_API_KEY=your-claude-api-key

# OpenAI (백업용)
OPENAI_API_KEY=your-openai-api-key
USE_3STAGE_PIPE=true

# ================================================================
# PubSub 설정 (기존 구조 유지)
# ================================================================
PUBSUB_TOPIC_NAME=ocr-processing
PUBSUB_SUBSCRIPTION_NAME=ocr-processing-sub

# ================================================================
# 파일 처리 설정
# ================================================================
TEMP_DIR=./temp
OUTPUT_DIR=./outputs
REPORTS_DIR=./reports
MAX_FILE_SIZE=104857600
MAX_FILES_PER_REQUEST=8

# ================================================================
# CORS & 보안 설정
# ================================================================
CORS_ORIGIN=http://localhost:5173,http://localhost:3000
ALLOWED_FILE_TYPES=application/pdf

# ================================================================
# 디버그 설정
# ================================================================
DEBUG_MODE=true
LOG_LEVEL=info
SKIP_PDF_TESTS=true

# ================================================================
# 사용량 제한 설정
# ================================================================
DEFAULT_MONTHLY_QUOTA=50
RATE_LIMIT_REQUESTS_PER_MINUTE=10
RATE_LIMIT_REQUESTS_PER_HOUR=100

# ================================================================
# 예약된 환경변수 (미래 확장용)
# ================================================================
# EMAIL_SERVICE_API_KEY=your-email-service-key
# SLACK_WEBHOOK_URL=your-slack-webhook
# SENTRY_DSN=your-sentry-dsn